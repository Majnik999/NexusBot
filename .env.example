token="YOUR_BOT_TOKEN"
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"
# Optional: language code for TTS (e.g., en, en-GB, en-US, fr, de)
VOICE_LANGUAGE="en"
# Optional: set a specific FFmpeg executable path. If not set, the bot will fall back
# to system PATH or download a portable binary via imageio-ffmpeg automatically.
FFMPEG_EXE=""